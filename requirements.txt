# Core AI
google-genai>=1.0.0
requests>=2.30.0
python-dotenv>=1.0.0

# Speech Recognition
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
sounddevice>=0.4.6
vosk>=0.3.45
# Скачать офлайн-модель речи (русский, лёгкая, ~50MB):
#   https://alphacephei.com/vosk/models -> vosk-model-small-ru-0.22
#   распакуй содержимое в models/small/ (должен получиться models/small/conf, models/small/am и т.д.)
# Без модели STT автоматически использует Google STT (нужен интернет).

# TTS — pyttsx3 (резерв, всегда работает)
pyttsx3>=2.90

# TTS — Silero (основной голос, офлайн, Python 3.12+, CUDA)
# torch устанавливается отдельно в start.bat с CUDA поддержкой
# soundfile нужен для Silero

soundfile>=0.12.0
librosa>=0.10.0
# librosa нужен для ресэмплинга звука до 48000 Hz перед воспроизведением -
# без этого на части аудио-устройств Windows (особенно через старый MME API)
# звук на 24000 Hz звучит искажённо/с "бульканьем"

# Utils
psutil>=5.9.0
PyYAML>=6.0
rapidfuzz>=3.0.0
pycaw>=20181226
numpy>=1.24.0

# Additional features
pyperclip>=1.8.0
screen_brightness_control>=0.4.0
deep_translator>=1.11.0
yt_dlp>=2023.0.0
opencv-python>=4.8.0
pywin32>=306
pyautogui>=0.9.54
pytesseract>=0.3.10
pyaudio>=0.2.14
pystray>=0.19.5
plyer>=2.1.0
pytest>=7.4.0
python-telegram-bot>=20.7
